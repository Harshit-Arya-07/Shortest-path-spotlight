{
  "name": "shortest-path-spotlight",
  "version": "1.0.0",
  "description": "Full-stack MERN application for visualizing shortest path algorithms",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "dijkstra",
    "bellman-ford",
    "astar",
    "graph",
    "algorithms",
    "visualization",
    "mern"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
